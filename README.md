# aksandco.net

Company homepage for AKS & Co. LLC. A single static page, served by GitHub Pages from the `main` branch root.

- `index.html` — the whole site (styles and the email-obfuscation script are inline)
- `CNAME` — custom domain for GitHub Pages
- `.nojekyll` — tells Pages to serve files as-is
